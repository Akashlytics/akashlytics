import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  graphExplanation: {
    fontSize: ".8rem",
    paddingTop: "1rem",
    fontStyle: "italic",
  },
}));